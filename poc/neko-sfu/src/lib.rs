//! Neko ↔ mediasoup PoC scratchpad.
//!
//! Real spike code lands incrementally in `src/bin/` as tasks 1.4–1.6 progress.
//! Cargo manifest declares no `[[bin]]` entries yet, so `cargo check` passes on
//! an empty crate (placeholder lib.rs keeps it well-formed).
